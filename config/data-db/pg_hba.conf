# TYPE  DATABASE  USER     ADDRESS         METHOD
local   all       all                      scram-sha-256
host    appdb     appuser  172.16.0.0/12   scram-sha-256
host    appdb     appuser  10.0.0.0/8      scram-sha-256
host    appdb     appuser  192.168.0.0/16  scram-sha-256
# Mặc định: từ chối mọi kết nối khác (implicit deny)
