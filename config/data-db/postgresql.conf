# PostgreSQL — miền DATA (tài sản quan trọng nhất)
listen_addresses = 'localhost'
port = 5432
max_connections = 20
ssl = off
log_connections = on
log_disconnections = on
password_encryption = scram-sha-256

# Least privilege: không cho superuser remote
superuser_reserved_connections = 3
